<odoo>
  <data>

        <record id="view_pdf_forms_form_form" model="ir.ui.view">
            <field name="model">pdf_forms.form</field>
            <field name="arch" type="xml">
                <form string="Form">
                    <header>
                        <button name="button_active" type="object" class="oe_highlight" string="Activate" attrs="{'invisible': [('state','=','active')]}"/> 
                        <button name="button_draft" type="object" string="Reset to Draft" attrs="{'invisible': [('state','=','draft')]}"/>
                        <button name="generate_pdf_field_mappings" type="object" string="Load PDF Fields" attrs="{'invisible': [('count_mapping','!=',0)]}"/>
                        <field name="state" widget="statusbar"/>
                        <field name="count_mapping" invisible="1"/>
                    </header>
                   <sheet>

                    <label for="name" class="oe_edit_only"/>
                    <h1>
                        <field name="name"/>
                    </h1>
                    <group>
                        <group>
                            <field name="datas" filename="datas_fname"/>
                            <field name="datas_fname" invisible="1" class="oe_inline oe_right"/>
                            <field name="model_id" required="1"/>
                            <field name="pdf_name_field" domain="[('model_id','=',model_id)]"/>
                        </group>
                        <group string="History" groups="base.group_no_one">
                            <label for="create_uid" string="Creation"/>
                            <div name="creation_div">
                                <field name="create_uid" readonly="1" class="oe_inline"/> on
                                <field name="create_date" readonly="1" class="oe_inline"/>
                            </div>
                        </group>
                    </group>
                    <separator string="Mappings"/>
                    <field name="mapping_ids" nolabel="1"/>
                  </sheet>
                </form>
            </field>
        </record>
        <record id="view_pdf_forms_form_tree" model="ir.ui.view">
            <field name="model">pdf_forms.form</field>
            <field name="arch" type="xml">
                <tree string="Form" decoration-success="state == 'active'" decoration-danger="state == 'draft'">
                    <field name="name"/>
                    <field name="datas_fname"/>
                    <field name="model_id" string="Model Applied To"/>
                    <field name="company_id" groups="base.group_multi_company"/>
                    <field name="create_uid"/>
                    <field name="create_date"/>
                    <field name="state"/>
                    <button name="toggle_state" type="object" icon="fa-power-off" string="Toggle State"/>
                </tree>
            </field>
        </record>
        <record id="view_pdf_forms_form_search" model="ir.ui.view">
            <field name="model">pdf_forms.form</field>
            <field name="arch" type="xml">
                <search string="Form">
                    <field name="name" filter_domain="['|', ('name','ilike',self), ('datas_fname','ilike',self)]" string="Form"/>
                    <field name="create_date"/>
                    <separator/>
                    <field name="model_id" string="Model Applied To"/>
                    <separator/>
                    <filter name="my_documents_filter"
                        string="My Forms(s)"
                        domain="[('create_uid','=',uid)]"
                        help="Filter on my forms"/>
                    <field name="create_uid"/>
                    <group expand="0" string="Group By">
                        <filter name="owner" string="Owner" domain="[]" context="{'group_by':'create_uid'}"/>
                        <filter string="Company" domain="[]" context="{'group_by':'company_id'}" groups="base.group_multi_company"/>
                        <filter string="Creation Month" domain="[]" context="{'group_by':'create_date'}"/>
                        <filter string="PDF Form Name" domain="[]" context="{'group_by': 'res_name'}"/>
                    </group>
                </search>
            </field>
        </record>


        <record id="action_pdf_forms_form" model="ir.actions.act_window">
            <field name="name">Forms</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">pdf_forms.form</field>
            <field name="view_type">form</field>
            <field name="view_id" eval="False"/>
            <field name="search_view_id" ref="view_pdf_forms_form_search"/>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    Click here to create new forms.
                </p>
            </field>
        </record>

        <menuitem name="PDF Forms" id="menu_pdf_forms_form" sequence="1" parent="menu_pdf_forms_form_main" action="action_pdf_forms_form"/>
        
        
        <record id="view_pdf_forms_server_action_form" model="ir.ui.view">
            <field name="name">PDF Forms Server Action</field>
            <field name="model">ir.actions.server</field>
            <field name="priority">100</field>
            <field name="arch" type="xml">
                <form string="PDF Forms Server Action">
                    <header>
                        <field name="binding_model_id" invisible="1"/>
                        <button name="create_action" string="Create Contextual Action" type="object"
                                class="btn-primary"
                                attrs="{'invisible':[('binding_model_id','!=',False)]}"
                                help="Display an option in the 'More' top-menu in order to run this action." invisible="1"/>
                        <button name="unlink_action" string="Remove Contextual Action" type="object"
                                attrs="{'invisible':[('binding_model_id','=',False)]}"
                                help="Remove 'More' top-menu contextual action related to this action" invisible="1"/>
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <label for="name" class="oe_edit_only"/>
                            <h1><field name="name" placeholder="e.g. Update order quantity"/></h1>
                        </div>
                        <group name="action_wrapper">
                            <group name="action_content">
                                <field name="model_id"/>
                                <field name="model_name" invisible="1"/>
                                <field name="state" invisible="1"/>
                                <field name="type" invisible="1"/>
                                <field name="crud_model_id"
                                    attrs="{'invisible': [('state', '!=', 'object_create')], 'required': [('state', '=', 'object_create')]}"/>
                                <field name="crud_model_name" invisible="1"/>
                                <field name="link_field_id"
                                    domain="[('model_id', '=', model_id), ('relation', '=', crud_model_name), ('ttype', 'in', ['many2one'])]"
                                    attrs="{'invisible': [('state', '!=', 'object_create')]}"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Python Code" name='code' autofocus="autofocus"
                                    attrs="{'invisible': [('state', '!=', 'code')]}">
                                <field name="code" widget="ace" options="{'mode': 'python'}" placeholder="Enter Python code here. Help about Python expression is available in the help tab of this document."/>
                            </page>

                            <page string="Data to Write" autofocus="autofocus"
                                    attrs="{'invisible':[('state', 'not in', ['object_create', 'object_write'])]}">
                                <p attrs="{'invisible': [('model_id', '!=', False)]}">
                                    Please set the Model to Create before choosing values
                                </p>
                                <field name="fields_lines">
                                     <tree string="Field Mappings" editable="bottom">
                                        <field name="col1" domain="['|', ('model_id', '=', parent.crud_model_id), ('model_id', '=', parent.model_id)]"/>
                                        <field name="type"/>
                                        <field name="value"/>
                                    </tree>
                                </field>
                            </page>

                            <page string="Actions" autofocus="autofocus"
                                    attrs="{'invisible': [('state', '!=', 'multi')]}">
                                <p class="oe_grey">
                                    If several child actions return an action, only the last one will be executed.
                                    This may happen when having server actions executing code that returns an action, or server actions returning a client action.
                                </p>
                                <field name="child_ids" domain="[('model_id', '=', model_id)]"/>
                            </page>

                            <page string="Help"
                                    attrs="{'invisible': [('state', '!=', 'code')]}">
                                <group>
                                    <div style="margin-top: 4px;">
                                        <h3>Help with Python expressions</h3>
                                        <p>Various fields may use Python code or Python expressions. The following variables can be used:</p>
                                        <ul>
                                            <li><code>env</code>: Odoo Environment on which the action is triggered</li>
                                            <li><code>model</code>: Odoo Model of the record on which the action is triggered; is a void recordset</li>
                                            <li><code>record</code>: record on which the action is triggered; may be be void</li>
                                            <li><code>records</code>: recordset of all records on which the action is triggered in multi mode; may be void</li>
                                            <li><code>time</code>, <code>datetime</code>, <code>dateutil</code>, <code>timezone</code>: useful Python libraries</li>
                                            <li><code>log(message, level='info')</code>:logging function to record debug information in <code>ir.logging</code> table</li>
                                            <li><code>Warning</code>: Warning Exception to use with <code>raise</code></li>
                                            <li>To return an action, assign: <code>action = {...}</code></li>
                                        </ul>
                                        <div attrs="{'invisible': [('state', '!=', 'code')]}">
                                            <p>Example of Python code</p>
<code style='white-space: pre-wrap'>
partner_name = record.name + '_code'
env['res.partner'].create({'name': partner_name})
</code>
                                        </div>
                                    </div>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>
        
        <record id="view_pdf_forms_server_action_tree" model="ir.ui.view">
            <field name="name">Server Actions</field>
            <field name="model">ir.actions.server</field>
            <field name="priority">100</field>
            <field name="arch" type="xml">
                <tree string="Server Actions">
                    <field name="name"/>
                    <field name="pdf_form_id"/>
                    <field name="model_id"/>
                </tree>
            </field>
        </record>
        
        <record id="action_pdf_forms_server_action" model="ir.actions.act_window">
            <field name="name">PDF Forms Server Actions</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">ir.actions.server</field>
            <field name="view_type">form</field>
            <field name="view_mode">tree,form</field>
            <field name="view_id" ref="view_pdf_forms_server_action_tree"/>
            <field name="context">{'key':'server_action', 'default_state': 'code', 'form_view_ref': 'pdf_forms.view_pdf_forms_server_action_form'}</field>
            <field name="domain">[('is_pdf_form','=',True)]</field>
        </record>
        
        <menuitem name="Server Action" id="menu_pdf_forms_server_action" sequence="3" parent="menu_pdf_forms_form_main" action="action_pdf_forms_server_action"/>
    </data>
</odoo>
